package org.mindustrytool;

public final class NekoModule {
    // Defines custom dependency injection providers for the Mod.
    // Standard classes with @Inject constructors are resolved automatically.
}
